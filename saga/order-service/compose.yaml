services:
  postgres:
    image: 'postgres:latest'
    container_name: 'orderdb'
    environment:
      - 'POSTGRES_DB=orderdb'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5431:5432'
