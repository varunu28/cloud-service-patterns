spring:
  application:
    name: orderservice

  datasource:
    url: jdbc:postgresql://localhost:5432/order-service
    username: order-user
    password: secret
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

server:
  port: 8001